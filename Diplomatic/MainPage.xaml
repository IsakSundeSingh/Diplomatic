<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Diplomatic"
             x:Class="Diplomatic.MainPage">
    <ContentPage.BindingContext>
        <local:InputFormViewModel/>
    </ContentPage.BindingContext>
    <ScrollView>
        <StackLayout>
            <!-- Place new controls here -->
            <Entry Placeholder="{Binding NameLabel}" Text="{Binding Name}"/>
            <DatePicker Date="{Binding Date}" MaximumDate="{Binding MaxDate}"/>
            
            <!-- For signatures -->
            <ListView ItemsSource="{Binding ListItems}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ImageCell ImageSource="{Binding Imgsrc}" Text="{Binding Name}" Detail= "{Binding Detail}" TextColor= "#2980b9" DetailColor="#27ae60"> </ImageCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <!-- Signature end-->
            
            <Button Text="Next"
                    BackgroundColor="#27ae60"
                    TextColor="#FFF"
                HorizontalOptions="Center"
                    IsEnabled="False" />
            
        </StackLayout>
        
    </ScrollView>
</ContentPage>
